function mostrar()
{
	//tomo la edad  
	var edad;
	edad = txtIdEdad.value;
	parseInt(edad);

	if (edad >12 && edad < 18) {
		

	alert (" es adolescente ")

	}
	else (edad )
 // operadores logicos && ( y ) || (o) ! ( not)

}//FIN DE LA FUNCIÓN